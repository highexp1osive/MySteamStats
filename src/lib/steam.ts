// Steam data fetch and sync (full implementation in task 3)

export async function syncGameLibrary(_userId: string, _steamId: string): Promise<number> {
  return 0;
}

export async function syncReviews(_userId: string, _steamId: string): Promise<number> {
  return 0;
}
